# Normalise line endings in the repository to LF, and check them out as LF
# everywhere. docs/example-results.csv is generated and a test compares the
# regenerated bytes against the committed ones, so a checkout that rewrote
# its line endings would fail that test on one platform and pass on another.
* text=auto eol=lf

*.png binary
